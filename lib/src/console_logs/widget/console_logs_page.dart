import 'package:flutter/material.dart';

class ConsoleLogsPage extends StatelessWidget {
  const ConsoleLogsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Condole logs page',
        ),
      ),
    );
  }
}
